apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: tracing
  namespace: dapr
  annotations:
    fluxcd.io/ignore: "false"
spec:
  tracing:
    enabled: true
    expandParams: true
    includeBody: true
