apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: appconfig
  namespace: dapr-app
  annotations:
    fluxcd.io/ignore: "true"
spec:
  tracing:
    enabled: true
    expandParams: true
    includeBody: true
